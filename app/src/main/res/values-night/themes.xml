<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcoReciclaAppMobile2024" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.YourAppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores primarios del tema -->
        <item name="colorPrimary">@color/text_secondary_dark</item>
        <item name="colorPrimaryVariant">@color/text_secondary_dark</item>
        <item name="colorOnPrimary">@color/text_primary_dark</item>

        <!-- Colores secundarios del tema -->
        <item name="colorSecondary">@color/text_secondary_dark</item>
        <item name="colorSecondaryVariant">@color/text_secondary_dark</item>
        <item name="colorOnSecondary">@color/text_primary_dark</item>

        <!-- Colores de superficie -->
        <item name="colorSurface">@color/card_background_dark</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary_dark</item>

        <!-- Color de fondo -->
        <item name="android:colorBackground">@android:color/transparent</item>

        <!-- Estilos para el botón -->
        <item name="colorButtonNormal">@color/button_background_dark</item>
        <item name="android:textColorPrimary">@color/button_text_dark</item>
    </style>
</resources>